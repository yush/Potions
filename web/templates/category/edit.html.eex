<h2>Edit category</h2>

<%= render "form.html", changeset: @changeset,
                        action: user_category_path(@conn, :update, @user, @category) %>

<%= link "Back", to: user_category_path(@conn, :index, @user) %>
